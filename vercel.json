{
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "framework": "vite",
  "ignoreCommand": "echo 'Use Vercel env vars for client only; server runs on Railway'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
